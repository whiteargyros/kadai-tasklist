<h3><%= @user.name %></h3>

<img class="rounded img-fluid" src="<%= gravatar_url(@user, { size: 100 }) %>" alt=""><br>

<h3>MyTasklist</h3>
<%= render 'tasks/tasks', tasks: @tasks %>

<%= link_to 'PostTasks', root_path, class: 'btn btn-lg btn-primary' %><br>
<%= link_to 'Logout', logout_path, method: :delete %>
