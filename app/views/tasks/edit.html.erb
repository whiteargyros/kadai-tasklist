<% if current_user == @task.user %>
  <h1>id: <%= @task.id %> edit</h1>

  <%= render 'form', task: @task %>
  <br>
  <%= link_to 'tasklist', root_path %>
  
<% else %>
  unable to edit task<br>
  <%= link_to 'Tasklist', root_path %>  
<% end %>